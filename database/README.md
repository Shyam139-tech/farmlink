# FarmLink database

`schema.sql` enables PostGIS and creates the marketplace, FPO, logistics, payment, AI, ledger, and notification tables. `seed.sql` adds core demo crops and FPOs. Run both against a PostgreSQL 16 + PostGIS instance.
